/**
 * Re-export desde src/hooks/usePaginacion.jsx
 * Mantener para compatibilidad con imports existentes.
 * Usar preferiblemente: import { usePaginacion } from "@/hooks/usePaginacion"
 */
export { usePaginacion } from "@/hooks/usePaginacion.jsx";
